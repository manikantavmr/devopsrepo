pwd file status
touch file - new file create
mkdir - new folder create
cd -enter into different folders
rm -rf --delete folder
cat --content of the file
cntrl +l  ----clear terminal
cntrl +C ----cancle the ternmianl
git init ---create an hidden file
git confg - global config user.name
git config -global user.email   ---------sets author email and name
t status ---list which files are staged, unstaged and untracked
git add filename or git add . ---unstaged to tracked or add files to staging area
git commit -m"" ----what changes we did in the files.

git log to view the history of commits for a repositary
git clone <repo url>----------clong of the repository download from github account
git push ---push the changes to the central reposatory
git show (conmit id) ---what the changes has done in file in speciic commit id
git pull -----to pull all changes to remote repostory into the branch
git branch --create new branch
git branch -b branch name --- crete branch name
git branch -- list ---already available branches
git branch -d brnch name -----delete branch
git brnach -D brname ------forcefully delete
git fork ----copy data of the main reposortory to resolve the issue bugs for helpful.
git checkout branch name ---to switch to another branch
git branch -m<old name ><new name>  -------rename the branch
============================================
git tag new tag 
git tag --list -----------available tages list will be reflect
git tag -a tag name -m"comments of the tag"
git tag -d tag name ---------tag delete

tag diff tag1 tag2

tag modifictaion   git tag -a tag name -f commit id new commit id
tag push ---------git push origin -----main repositery
git push origin tag name one tag
how many tags are added in repostitery checking v1.*


